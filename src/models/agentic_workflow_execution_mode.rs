/*
 * Qovery API
 *
 * - Qovery is the fastest way to deploy your full-stack apps on any Cloud provider. - ℹ️ The API is stable and still in development.
 *
 * The version of the OpenAPI document: 1.0.4
 * Contact: support+api+documentation@qovery.com
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// AgenticWorkflowExecutionMode : Where a run executes. IN_PLACE deploys the workflow in the environment it belongs to, and is the default. CLONE_ENVIRONMENT gives each run a throwaway copy of the whole environment, which is the only mode that isolates concurrent runs from each other.
/// Where a run executes. IN_PLACE deploys the workflow in the environment it belongs to, and is the default. CLONE_ENVIRONMENT gives each run a throwaway copy of the whole environment, which is the only mode that isolates concurrent runs from each other.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum AgenticWorkflowExecutionMode {
    #[serde(rename = "IN_PLACE")]
    InPlace,
    #[serde(rename = "CLONE_ENVIRONMENT")]
    CloneEnvironment,
}

impl std::fmt::Display for AgenticWorkflowExecutionMode {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        match self {
            Self::InPlace => write!(f, "IN_PLACE"),
            Self::CloneEnvironment => write!(f, "CLONE_ENVIRONMENT"),
        }
    }
}

impl Default for AgenticWorkflowExecutionMode {
    fn default() -> AgenticWorkflowExecutionMode {
        Self::InPlace
    }
}
